{
  "extends": "../tsconfig.base.json",
  "exclude": ["./functions"],
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    // Astro will directly run your TypeScript code, no transpilation needed.
    "noEmit": true
  }
}
